extends common/header
block body
	#loader
	.container-fluid.relative.fill-screen.registerBody.content
		.row
			.col-md-4.col-md-offset-4
				.panel.panel-default
					.panel-heading
						h1 ICU Register
					.panel-body
						form(action='/register', method='post')
							.form-group
								input.form-control(name='email', type='email', placeholder='Email', required='required')
							.form-group
								input.form-control(name='password', type='password', placeholder='Password', required='required')
							.form-group
								input.form-control(name='passwordCheck', type='password', placeholder='Password Check', required='required')
							button.btn.btn-primary.btn-lg.btn-block#registerButton(type='button')
								| Register
.modal.fade#alert(tabindex='-1', role='dialog')
	.modal-dialog(role='document')
		.modal-content
			.modal-header#alertHeader
				button.close(type='button', data-dismiss='modal', aria-label='Close')
					span(aria-hidden='true') ×
				h4.modal-title
			.modal-body#alertMessage
				p
			.modal-footer
				button.btn.btn-default(type='button', data-dismiss='modal') Close